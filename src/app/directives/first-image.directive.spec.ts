import { FirstImageDirective } from './first-image.directive';

describe('FirstImageDirective', () => {
  it('should create an instance', () => {
    const directive = new FirstImageDirective();
    expect(directive).toBeTruthy();
  });
});
