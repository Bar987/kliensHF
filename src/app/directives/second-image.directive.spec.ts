import { SecondImageDirective } from "./second-image.directive";

describe("SecondImageDirective", () => {
  it("should create an instance", () => {
    const directive = new SecondImageDirective();
    expect(directive).toBeTruthy();
  });
});
